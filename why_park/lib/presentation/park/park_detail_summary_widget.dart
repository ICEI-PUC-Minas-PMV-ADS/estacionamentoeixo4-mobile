
import 'package:flutter/material.dart';
import 'package:why_park/presentation/park/view_model/park_view_model.dart';

class ParkDetailSummaryWidget extends StatelessWidget {
  const ParkDetailSummaryWidget(Key? key, this._parkViewModel) : super(key: key);

  final ParkViewModel _parkViewModel;
  @override
  Widget build(BuildContext context) {
    return Card();
  }
}